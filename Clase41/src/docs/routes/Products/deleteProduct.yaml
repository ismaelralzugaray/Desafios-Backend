paths:
  /api/products/{id}:
    delete:
      summary: Elimina un producto segun el ID brindado
      tags:
        - Productos
      parameters:
      - name: id
        in: path
        description: path parameter takes the product id
        required: true
        type: string
      responses:
        "200":
          description: El producto fue eliminado con exito
        